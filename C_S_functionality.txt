Server:
- recieve a message from client (rear)
- if -->   message.decode() == "INIT_SYN"
		- Send back INIT_SYNACK, DATA = "VOID"
- elif --> message.decode() == "INIT_ACK"
		- Send back FULL_DATA_ACK, DATA = "VOID"
- elif --> message.decode() == "FULL_DATA_SYN"
		- if DATA = "CAM"
			- reset values 
			- decode full string, display image
			- Send back FULL_DATA_ACK, DATA = "CAM"
		- elif DATA = "SEN"
			- TEMP: Send back FULL_DATA_ACK, DATA = "SEN"
- elif --> message.decode() == "SYNC_SYN"
		- split data by '!'
		- store data_type (CAM or SEN) and SegmentSize (SS)
		- Send back SYNC_ACK, DATA = "DATA_TYPE!SS"
- elif --> message.decode() == "DATA_SYN"
		- split data by '!'
		- store data_type anod other_data
		- if data_type == "CAM"
			- SegmentSize = int(otherData)
		- elif data_type == "SEN"
			- TEMP:Send back DATA_ACK, DATA = "SEN!VOID"
- elif --> message.decode() == "DATA_CAM"
		- handle data, append string 
- elif --> message.decode() == "DATA_SEN"
		- handle sensor data
		- TEMP: print statement

Client:
- Send INIT_SYN
- Enter while loop
	- Receive message from server (front)
	- if message.decode() == INIT_SYNACK
		- Send back INIT_ACK, DATA = "VOID"
	- elif message.decode() == SYNC_ACK
		- split data
		- if data_type == "CAM"
			- start sending DATA_CAM
			- if checkpoint reached
				- Send DATA_SYN, DATA = "CAM!SS"
				- Wait for DATA_ACK, DATA = "CAM!PACKET_LOSS"
				- if packet loss
				    - Resend DATA_CAM
				    - Send DATA_SYN, DATA = "CAM!SS"
				    - Wait for DATA_ACK, DATA = "CAM!PACKET_LOSS"
				    (keep looping until no packet loss)
			- once all packets sent
				- Send FULL_DATA_SYN, DATA = "CAM"
		- elif data_type == "SEN"
			- TEMP: Send DATA_SEN, DATA = "LS!RS"
			- TEMP: Send DATA_SYN, DATA = "SEN!VOID"
			- TEMP: Wait for DATA_ACK
			- TEMP: Send FULL_DATA_SYN, DATA = "SEN"
	- elif messsage.decode() == FULL_DATA_ACK
		- if data == "VOID" or data == "SEN"
			- encode image, create encode_msgs[]
			- store SS
			- Send SYNC_SYN, DATA = "CAM!SS"
		- elif data == "CAM"
			- TEMP: Send SYNC_SYN, DATA = "SEN!0"
